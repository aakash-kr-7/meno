# ==============================================================================
# (a) What this file is: Package initializer for CLI subcommands.
# (b) What it does: Exposes CLI subcommands to the main entry point.
# (c) How it fits into the MENO system: Exposes the CLI application modules.
# ==============================================================================
